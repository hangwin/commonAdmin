export enum LOGIN {
	LOGIN = 'login',
	REGISTER = 'register',
}

export enum HOME {
	HOME = 'home',
}

export enum DASHBOARD {
	DASHBOARD = 'dashboard',
	SUMMARY = 'summary',
	WORKBENCH = 'workbench',
}
export default {
	LOGIN,
	HOME,
	DASHBOARD,
};
